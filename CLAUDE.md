# 롯데케미칼 SAP S/4HANA 통합 ERP 고도화 발표자료

광운대학교 경영대학 ERP개론 팀프로젝트.

이 파일은 Claude Code 세션의 **진입점**이다. 작업 전 반드시 이 파일을 먼저 읽고, 해당 작업에 필요한 `spec/` 문서를 순서대로 확인한다. 세부 규칙과 충돌할 경우, **`CLAUDE.md`의 절대 규칙을 우선**한다.

본 파일은 진입점만 유지하기 위해 §12 변경 이력 누적·§13-4 가드 누적·§13-9~14 진입 누적·§14-6 git push 흐름을 외부화한다. 상세는 §7·§12·§13·§14 안내 참조.

---

## 0. 현재 작업 단계

**Phase 1: 디자인 시스템 확정 (완료)** → **Phase 2: 발표 기획 (완료)** → **Phase 3: 슬라이드 명세 + 빌드 (진행 중)**

### 확정된 것
- 디자인 토큰 (`shared/style.css`)
- 컴포넌트 라이브러리 일부 (`shared/style.css`)
- Tone A/B/C 위계 시스템 개념 (`spec/tone-system.md`에서 정밀화 예정)
- Tone A 표지 시안 (`slides/01-cover.html`)
- 원본 보고서 `docs/Step1_v4.docx` ~ `docs/Step5_v4.docx`
- 16:9 스테이지·카운트업·reveal 컨트롤러 (`shared/script.js`)
- 핵심 메시지 카피 (`spec/content-thesis.md` §1)
- 4개 보조 논지 (M&A 통합 압력 / 산업 특성과 시스템 차이 / 트레이드오프 관리 / SAP 중심 플랫폼)
- 발표 구조 — docx Step 1~5 순서를 챕터 단위로 따름. 표지(01) + 발표 흐름 미리보기(02 toc) + Step 1~5 챕터 본론 + 결론
- **현재 페이지 분배 (v2.14 기준·총 49장)**: Step 1 10장 (01~10) + Step 2 11장 (11~19·chapter2-intro 포함) + **Step 3 14장 (20~33·v10.13 확정)** + Step 4 8장 (34~41) + Step 5 8장 (42~49)
- 청중 노출용 페이지 번호는 단독 표기·분모 비노출 (가드 8)
- 결론은 49 final-conclusion (Step 5 §7) 단일 페이지로 회수

### v2.14 기준 미진입 항목 (다음 차수 결정)
- Step 3 디자인 1차 재구성 (`dist/preview-step3.html` v1 → v2·14장 빌드)
- Step 4·5 dist 헤더 페이지 번호 시프트 (+7)
- slides.yml 골조 보정 (28장 → 49장)
- Step 2 잔여 보정 + hp-num 일괄 + content-thesis §3 매핑 표 페이지 번호 갱신 (발표 직전 일괄)

### 아직 금지
- `shared/script.js` 수정
- 디자인 시스템 전면 변경
- `spec/` 문서 임의 수정
- 분모(총 장수) 청중 노출

## 1. 프로젝트 정의

- **주제**: 롯데케미칼의 M&A 이후 3사 통합 ERP 구축 (2017.04 — 2017.11, 7개월)
- **분석 대상**: 롯데케미칼 / 롯데정밀화학 / 롯데첨단소재
- **솔루션**: SAP S/4HANA (10개 핵심 모듈)
- **글로벌 범위**: 해외 거점 6개국 · 110개국 수출
- **청중**: 경영학과 학부생 + 교수 (ERP개론 수강생)
- **발표 환경**:
  - 실행 기준: 로컬 브라우저에서 합본 HTML 직접 실행
  - 우선 브라우저: Chrome 또는 Edge
  - 발표 조작: 키보드 F (전체화면), R (애니메이션 재생), 방향키 (슬라이드 이동)
  - PDF 변환은 후순위. 우선은 화면 발표 최적화

## 2. 핵심 메시지

> 이 사례는 단순한 ERP 도입을 넘어,
> **M&A 이후 3사를 하나의 운영 체계로 묶기 위한 통합 경영 기반 구축 프로젝트**다.

## 3. 절대 규칙 (위반 = 작업 중단)

다음은 어떤 경우에도 위반할 수 없다.

1. **다크 톤 사용 — 환기·강조판 용도 한정**

   슬라이드의 주 배경(stage 전체)은 흰색(`--color-paper`) 또는 따뜻한 오프화이트(`--color-bg`, `--color-bg-cream`, `--color-bg-warm`)만 사용한다. 슬라이드 전체를 다크 톤으로 채우는 풀블리드 다크 배경은 금지한다.

   다크는 슬라이드의 핵심 색상이 아니다. 청중의 1차 시선이 머무는 영역(핵심 메시지 박스 / 챕터 진입 주 영역 / 본문 메인 카드)에는 다크 배경을 사용하지 않는다. 다크는 환기·강조판 용도로만 사용한다.

   본 조항은 면적 수치(%) 기준이 아닌 시선 위계 기준으로 판단한다.

   **허용 용도 (6개)**
   - 헤더 띠 (다크 영역 카운트 제외 · Tone 공통 헤더 안전 영역)
   - footer 띠 (선택적)
   - 레드 강조의 배경판 — 단일 KPI 카드 내부 보조 배경판 1개
   - 도식 노드 한 개 (`.integration-svg` 코어 노드 등)
   - **다크 강조판 영역** (`.dark-panel`) — **한 슬라이드 다크 영역 1개 엄격 제한** (v1.7). 본문 메인 영역 제외
   - **(v1.7 재정의) `.red-hero-block`** — 빨간 fill 폐기. white 배경 + 좌측 12px red border-left + 큰 숫자 watermark + 다크 텍스트 구조

   **금지 용도 (4개)**
   - 핵심 메시지 박스 다크 처리 (표지·결론·논리 지도)
   - 챕터 진입의 주 영역 다크 (`.chapter-cover` 좌측 띠·상단 띠 다크 X — cream 톤 또는 오프화이트)
   - 본문 메인 카드 다크 (Tone B 분석 카드·Tone C 데이터 카드·Tone A KPI 임팩트 카드)
   - 슬라이드 좌우 어느 한쪽이 "절반 인상"을 주는 다크 띠

2. **16진수 색상 하드코딩 금지** — 색상은 반드시 `var(--color-*)` 또는 `currentColor`를 사용한다. 예외: `fill="none"`, `stroke="none"`은 허용. SVG 픽토그램 내부 fill/stroke도 var(--color-*) 토큰 사용. inline style 16진수 금지.

3. **영어 장식 라벨 금지** — 의미 전달보다 분위기 연출을 위해 쓰는 영어 단어를 사용하지 않는다. 금지 예시: `CASE STUDY`, `DIGITAL CORE`, `LEAD ENTITY`. 허용 용어 목록은 `spec/language-policy.md` 참조. "Upstream/Midstream/Downstream" 같은 산업 분류 용어는 한글 병기 시 허용 (예: "Upstream · 상류"). 원소기호(C₂H₄ 등)는 도메인 시각 어휘로 허용.

4. **한 슬라이드 핵심 메시지 1개** — 두 개 이상으로 갈라지면 슬라이드를 쪼갠다.

5. **근거 없는 내용 추가 금지** — outline 또는 docs에 없는 수치·주장 생성 금지. 단, outline 누락 또는 수치 충돌이 의심되면 작업을 중단하고 사용자 승인 후 `docs/` 원문을 확인한다.

6. **`shared/script.js` 개별 슬라이드 작업 중 수정 금지** — 합본 단계에서 진행 바·해시 이동·발표자 조작 개선이 필요할 때만 사용자 승인 후 수정 가능.

7. **다이어그램의 3사 구조 변경 금지** — `.integration-svg` 노드는 항상 롯데케미칼/정밀화학/첨단소재 + 코어 4개로 고정.

8. **분모(총 장수) 청중 노출 금지** — 슬라이드 헤더 우측 페이지 번호는 단독 표시(예: "04"), 분수 표시(예: "04 / 30") 금지.

## 4. 권장 규칙 (필요 시 사용자 승인 후 예외 가능)

- 레드 강조 포인트는 한 페이지에 3개 이하
- KPI 카드는 한 페이지에 4개 이하
- 페이지번호는 ink-soft 사용 (강조 필요 시 레드 가능)
- 새 컴포넌트보다 기존 컴포넌트 우선
- HTML 작업 중 최소 CSS 보정 허용 (변경 사항 기록)
- footer 영역은 우하단 키보드 안내만 유지 (좌하단·중앙 footer 정보 제거)

### 4-1. 슬라이드 레이아웃 표준 (v1.6)

- stage 1920×1080 고정
- padding: **52px 76px 64px**
- 헤더 영역 높이: **108px** (본문 영역 침범 금지)
- 본문 영역 height: `calc(1080px - 52px - 64px - 108px) = 856px`
- 로고 사이즈: Tone A 100% (104px) / Tone B·C 60~70% (64~72px)
- footer: 우하단 키보드 안내만 (28~36px·본문 영역 침범 X)
- Tone별 padding 토큰 override (`spec/design-tokens.md` §4-3 참조)

### 4-2. 카드 내부 밀도 표준 (v1.6)

- 카드 패딩: 28px 32px (기본) / 22px 28px (밀도형)
- 카드 내부 요소 최소 크기: 아이콘 36~44px / 카드 제목 30~36px / 본문 22~24px
- 카드 외형 크기 vs 내부 요소 비례 정합 (큰 카드 + 작은 텍스트 = 금지)
- 카드 내부 빈 공간이 카드 면적의 40% 초과 금지

### 4-3. 색상 비율 표준 (v1.7)

| 색상 | 권장 비율 | 역할 |
|---|---|---|
| White / Paper | 70~80% | 기본 배경 |
| Graphite / Black | 15~22% | 제목·카드 헤더·구조선 |
| Red | 3~7% | 핵심 강조·번호·핀·얇은 선·핵심 단어 (면 X) |
| Cream / Light Gray | 5~10% | 보조 배경·데이터 카드·활성 표시 |

**빨강은 면이 아닌 신호등 역할**로만 사용한다. 빨강 허용 = 핵심 단어·작은 라벨·번호 마커·얇은 선·지도 핀·큰 숫자 watermark. 빨강 금지 = 30% 이상 큰 면·한 슬라이드 5개 초과·빨강 + 다크 동시 큰 면.

### 4-4. 전역 타이포 스케일 강제 룰 (v1.8)

발표용 1920×1080 슬라이드 기준. 페이지별 감으로 글자 크기 정하지 말고 본 룰 우선 적용 (`spec/design-tokens.md` §2-3).

| 요소 | 최소 크기 | 권장 크기 |
|---|---|---|
| 페이지 메인 제목 | 48px | 56~64px |
| 챕터 제목 | 64px | 72~86px |
| 카드 제목 | 30px | 34~40px |
| 카드 본문 | 22px | 24~26px |
| 보조 설명 | 20px | 22px |
| 라벨·eyebrow | 16px | 18~20px |
| 표 본문 | 26px | 28~30px |
| 숫자 KPI | 96px | 110~140px |
| 작은 원 번호 | 30px 원 내부 16px | 42~48px 원 내부 20px |
| 의미 전달용 아이콘 | 36px | 44~56px |

강제 룰: 최소 크기 미만 금지. 컨테이너가 크면 내부 텍스트·아이콘도 함께 키운다. 70% 축소해서 카드 제목이 읽히지 않으면 실패.

### 4-5. 레이아웃 비율 강제 룰 (v1.8)

1. 왼쪽 요약 패널 정보 2줄 이하라면 화면 폭의 30% 초과 금지
2. 목차·소목차에서 항목 리스트가 핵심이면 화면 높이의 70% 이상 사용
3. 카드 내부 텍스트 2줄 이하라면 카드 높이 축소 또는 내부 요소 확대
4. 지도·다이어그램은 화면에 크게 + 핵심 숫자 배지(data-badge) 동반
5. 카드 잘림·화학식 삐져나감 즉시 수정 (overflow·카드 높이)
6. 50:50 분할 기본값 금지. 30:70 / 35:65 / 40:60 사용

## 5. 작업 중단 조건 (사용자 확인 필요)

다음 상황에서는 임의 판단하지 말고 정지한다.

1. outline과 원본 보고서의 수치 충돌
2. 슬라이드 핵심 메시지가 2개 이상으로 갈라짐
3. 새 컴포넌트가 필요한데 기존으로 대체 불가
4. CSS 토큰 추가 필요
5. 영어 용어 사용 여부 애매함
6. 발표 구성·장수 변경이 필요함
7. 기존 디자인 규칙을 깨야 더 나은 결과가 나옴

### 정지 시 출력 형식

```
─────────────────────────────────
🛑 작업 중단

중단 사유:
충돌한 규칙:
판단이 필요한 선택지:
  A.
  B.
추천안:
사용자에게 필요한 확인:
─────────────────────────────────
```

## 6. 작업 컨텍스트 로드 규약

### 6-1. Phase 3 (슬라이드 빌드) 시 필수 로드

1. `CLAUDE.md` (이 파일)
2. **`spec/step1-canonical-patterns.md` v1.1** (Step 2~5 작업 우선 기준)
3. `outline/slides.yml` (해당 슬라이드 spec)
4. `outline/NN-name.md` (해당 슬라이드 기획서)
5. `spec/component-catalog.md` (컴포넌트 사용 기준·canonical과 충돌 시 canonical 우선)
6. `spec/tone-system.md` (Tone 분류 및 배치)
7. `spec/content-thesis.md` (핵심·보조 논지)

### 6-2. 필요 시 로드

- `spec/language-policy.md` — 영어 용어 사용 시
- `spec/design-tokens.md` — 색상·CSS 토큰 확인 또는 수정 시
- `spec/quality-checklist.md` — 빌드 후 검수 시
- `spec/guards.md` — 가드 활성/비활성 상태 또는 누적 등록 확인 시
- `spec/session-log.md` — 직전 차수 진입 누적 확인 시
- `spec/git-push-flow.md` — Step 마감 시점 push 사전 보고 작성 시

### 6-3. 톤별 참조 템플릿

- **Tone A** (표지·챕터 진입) — 표준 **01 cover**. 큰 빨간 면 X. 작은 라벨·핵심 단어·다이어그램 액센트선·KPI 보조선만 빨강.
- **Tone B** (분석 설명) — 표준 **05 financial-context** + 보조 **04 analysis-target**. 비율 60:40 또는 40:60 (50:50 X).
- **Tone C** (데이터·표) — 표준 **08 info-constraints**. 표 헤더 graphite·셀 키워드 중심.

### 6-4. `docs/*.docx` 원문 참조 규약

**Phase 3 (슬라이드 빌드)**: 직접 참조 제한. outline을 통해서만 참조. 수치 충돌·출처 확인 요청·outline 누락 발견·사용자 지시 시에만 사용자 승인 후 재참조.

## 7. spec/ 디렉토리 가이드

| 파일 | 역할 | 작성 상태 |
|---|---|---|
| `design-tokens.md` | 색상·타이포·스페이싱 토큰 정의 | v1.6 |
| `component-catalog.md` | 컴포넌트별 사용 기준·금지 사항 | v1.6 |
| `tone-system.md` | Tone A/B/C 분류 + 슬라이드 배치 | 분포 결정 / 명세 v1.0 작성 권장 |
| `content-thesis.md` | 핵심 메시지 + 보조 논지 + 메시지 ↔ 슬라이드 매핑 | §1·§2·§4 작성됨 |
| `language-policy.md` | 영어 용어 허용 목록 + 사용 방식 | 미작성 |
| `quality-checklist.md` | 발표자료 품질 기준 + 빌드 후 self-check | 미작성 |
| `guards.md` | 가드 11~89 누적 + 활성/비활성 상태 + 영구 가드 명시 | v2.17 분리 신설 |
| `session-log.md` | §13-9~14 차수별 진입 누적 (Step 1 GPT 피드백·Step 3 빌드·Step 4·5 §3·Step 1 v12·v13) | v2.17 분리 신설 |
| `git-push-flow.md` | §14-6 git push 운영 흐름 상세 (가드 66 영역) | v2.17 분리 신설 |

## 8. 디렉토리 구조

```
lotte-erp/
├── CLAUDE.md              ← 이 파일 (진입점)
├── spec/                  ← 디자인·콘텐츠 헌법 (정적)
├── docs/                  ← 원본 docx + changelog.md (변경 이력 외부화 보관)
├── outline/               ← 슬라이드 명세 (Phase 2 산출물)
├── slides/                ← 슬라이드 HTML (Phase 3 산출물)
│   └── 01-cover.html      (시안 완료)
├── shared/                ← 공통 자원 (CSS·JS·로고)
├── dist/                  ← 합본 빌드 결과
└── .claude/               ← Claude Code 설정 (에이전트·커맨드·후크)
```

### 8-1. 합본 방식

초기에는 **수동 합본을 원칙**으로 한다. 자동 빌드 스크립트(Vite, Webpack 등)가 필요해질 경우 사용자 승인 후 `tools/` 또는 `scripts/` 디렉토리를 생성한다. Claude Code가 임의로 빌드 환경을 만들지 않는다.

## 9. 표준 작업 흐름 (Phase 3 슬라이드 빌드)

1. **`outline/slides.yml`**에서 해당 슬라이드의 목적, tone, must_include 확인
2. **`outline/NN-name.md`**에서 발표자 메시지와 시각화 방향 확인
3. **기존 컴포넌트로 구현 가능한지** `spec/component-catalog.md` 확인
4. **HTML 작성** (`slides/NN-name.html`)
5. **필요한 경우 최소 CSS 보정** (`shared/style.css`) — 변경 사항 기록
6. **`spec/quality-checklist.md`** 기준으로 self-check
7. **변경 요약 출력** — 추가된 컴포넌트, 수정된 토큰, must_include 충족 여부

### 9-1. 완성 전 금지
- 곧바로 `dist/` 합본 수정
- 다른 슬라이드 동시 수정
- `shared/script.js` 수정
- outline에 없는 내용 추가

## 10. spec/ 문서 수정 규칙

- 사용자의 **명시적 요청 없이** `spec/` 문서를 수정하지 않는다
- 슬라이드 제작 중 규칙 충돌이 발견되면 spec을 임의 수정하지 말고 사용자에게 보고한다 (§5)
- 사용자가 승인한 규칙 변경은 **해당 spec 파일**과 **`CLAUDE.md` §12 변경 이력**에 함께 기록한다

## 11. 절대 금지 패턴 (실제 사례)

- ❌ `LEAD ENTITY · 01`, `SUBSIDIARY · 02` 같은 영어 장식 라벨 추가
- ❌ `<rect ... fill="#E60012">` 처럼 색상 하드코딩
- ❌ "ERP 도입 효과"로 끝나는 결론 (핵심 메시지에서 이탈)
- ❌ 한 페이지에 KPI 카드 5개 이상
- ❌ 다이어그램에 새 노드 추가 (3사 + 코어 구조 고정)
- ❌ 슬라이드 전체를 다크 톤으로 채우는 풀블리드 다크 배경
- ❌ `style="color: red"` 인라인 스타일
- ❌ 슬라이드별로 폰트 패밀리 변경
- ❌ 헤더 우측에 분모(총 장수) 노출 (예: "04 / 30" 형식 금지, "04" 단독 표시만 허용)
- ❌ 핵심 메시지 박스를 다크 배경으로 처리 (표지·02번·결론)
- ❌ 챕터 진입의 주 영역을 다크 띠로 처리
- ❌ 본문 메인 카드를 다크 배경으로 처리
- ❌ 한 슬라이드에 다크 강조판 2개 이상 동시 노출
- ❌ 슬라이드 좌우 한쪽이 "절반 인상"을 주는 다크 띠 점유
- ❌ footer 영역에 페이지 라벨·학교명 중복 노출
- ❌ 한 슬라이드에 다크 + red-hero 합산 3개 이상 (헤더 띠 카운트 제외)
- ❌ (v1.6) 카드 외형 크기 vs 내부 요소 크기 불일치
- ❌ (v1.6) 50:50 절반 분할 강박
- ❌ (v1.6) 헤더 영역 본문 영역 침범 (헤더 높이 108px 초과)
- ❌ (v1.6) 본문 영역 잘림 (overflow visible 또는 본문이 stage 하단 초과)
- ❌ (v1.6) 카드 내부 빈 공간 40% 초과
- ❌ (v1.7) 빨간 큰 면 사용 (red fill 30% 이상 차지)
- ❌ (v1.7) 빨강 강조 포인트 한 슬라이드 5개 초과
- ❌ (v1.7) 빨강 + 다크 동시 큰 면 사용
- ❌ (v1.8) 페이지 메인 제목 48px 미만 / 카드 제목 30px 미만 / 카드 본문 22px 미만 / 표 본문 26px 미만 / 라벨·eyebrow 16px 미만 / 의미 전달용 아이콘 36px 미만 / 목차 카드 번호 원 42px 미만
- ❌ (v1.8) 컨테이너만 크고 내부 글자·아이콘이 작은 패턴
- ❌ (v1.8) 왼쪽 패널 정보 2줄 이하인데 폭 30% 초과
- ❌ (v1.8) 목차 페이지에서 항목 리스트가 화면 높이 70% 미달

## 12. 변경 이력

본 차수 + 최근 2차수만 본체에 유지. v2.14 이하 전 영역은 `docs/changelog.md` 참조.

- 2026.05.29 v2.18 · **dist 페이지 번호 01~57 연속 정합 + 가드 65·66 해제 차수**. dist preview-step2~5 hp-num 재정렬(step2 11~21·step3 22~36·step4 37~47·step5 48~57·step1과 합쳐 01~57 연속) + step1 목차 카드 4건(장수·페이지 범위) + step4 흐름카드 배지 6·주석 6(+2) + step5 본문 안내 2건("다음 50/51 페이지"·+4). 총 19곳 + hp-num 헤더. dist preview-step1~5 5개 파일 커밋·push (`6472609`). **가드 65 해제** (사용자 명시 승인): Bash 환경 오류 해소 → Bash 도구 직접 사용 허용·§11 Bash 실행 규칙만 적용. **가드 66 실행 주체 제한 해제**: Claude Code가 git push 직접 수행 가능·push 시점 규칙(66-1·66-2·66-3)은 유지. `spec/guards.md` §0·v2.7·v2.18 정합. dist 디자인·본문 시각 영역 변경 X (페이지 숫자만). 다음 차수: CLAUDE.md·spec "49장" → "57장" 정합.
- 2026.05.26 v2.17 · **CLAUDE.md 분리 작업** (본인 디렉션 정합·대안 A 채택). 토큰 절감 + 진입점 정합 복원 목적. **신규 파일 4건**: `docs/changelog.md` (§12 v1.0~v2.14 외부화)·`spec/guards.md` (§13-4 가드 11~89 누적 외부화·영구 가드 명시)·`spec/session-log.md` (§13-9~§13-14 진입 누적 외부화)·`spec/git-push-flow.md` (§14-6 git push 흐름 외부화). **CLAUDE.md 본체 압축**: 약 1093줄 → 약 300줄. §0~§11·§13-1~§13-8·§14-1~§14-5 구조 유지·세부 표·픽토그램 어휘 등 핵심 정합 보존. §7 spec/ 디렉토리 가이드에 신규 3건 추가. §6-2 필요 시 로드에 신규 3건 추가. §12 변경 이력은 v2.17·v2.16·v2.15 본체 유지·이전 영역 `docs/changelog.md` 안내. §13-4 가드 누적 → `spec/guards.md` 안내. §13-9~§13-14 → `spec/session-log.md` 안내. §14-6 → `spec/git-push-flow.md` 안내. 디자인 요소 보호 (가드 53·45·49) 정합 ○. 표현 금지 5건 회피 (가드 11) 정합 ○. Bash 완전 배제 (가드 7·65) 정합 ○. dist/* 빌드 변경 X (모든 빌드 보호). 백업 생략. 다음 차수: 디자인 영역 또는 발표 직전 영역 (Step 2 잔여·출처 §11 통합·hp-num 일괄·slides.yml 골조).
- 2026.05.25 v2.16 · **Step 4·5 안내 페이지 본문 신규 + 본론 16장 헤더 시프트 + content-thesis §3 갱신 차수 — 문서 정합만·dist 변경 X**. 매트릭스 §9-NEW Step 4 안내 (35 step4-intro-overview) + Step 5 안내 (44 step5-intro-overview) 본문 신규 작성. Step 4·5 본론 16장 헤더 페이지 번호 시프트 (v10.13 §12-4 시프트 기록만 → v10.14 §9-NEW 헤더 영역 실제 갱신·Step 4: 27→36 ~ 34→43·Step 5: 35→45 ~ 42→52). content-thesis §3 매핑 표 갱신 (보조 논지 1·3·4 회수 페이지 v10.14 정합). master §1 Step 4·5 본문 요약 동기화. dist 빌드 변경 X. 가드 88·89 본 차수 등록. 다음 차수: Step 4·5 dist 디자인 1차 재구성.
- 2026.05.25 v2.15 · **Step 4·5 9장 확정 차수 — 문서 정합만·dist HTML 변경 X**. Step 4 9장 (35~43·step4-intro-overview 신규 + 본론 8장 페이지 시프트 +1)·Step 5 9장 (44~52·step5-intro-overview 신규 + 본론 8장 시프트 +1). 43 pir-conclusion 한계 5건 보조 표 축소·52 final-conclusion 핀잡 시각 주인공·출처 13건 source-line 압축. Step 4·5 한계 관점 분리 (Step 4 = PIR 검증 가능 범위·Step 5 = 일반화 트레이드오프). 매트릭스 v10.13 → v10.14·content-thesis v10.13 → v10.14·master v1.3 → v1.4. dist 빌드 영역 변경 X. 가드 85·86·87 본 차수 등록. 다음 차수: Step 4·5 dist 디자인 1차 재구성.

이전 변경 이력 (v1.0 ~ v2.14)은 `docs/changelog.md` 참조.

---

## §13. 세션 운영 가이드

본 프로젝트는 외부 시점 + GPT + Claude Code + 본인(운영 본부) 3분할 운영 구조이며, 본인이 여러 세션에 걸쳐 진행하는 장기 프로젝트이다.

### 13-1. 세션 진입 시 우선 참조 파일

1. `CLAUDE.md` (본 파일·현재 버전 확인·§12 변경 이력 검토)
2. `SESSION_STATE.md` (본인 환경 루트·세션 간 진행상황 정리)
3. `outline/step-page-matrix.md` (현재 매트릭스 버전·페이지별 §3 가이드)
4. `outline/_v*-build-guide.md` (현재 차수 또는 다음 차수 보조 가이드 — 있다면)
5. `spec/content-thesis.md` (§4 표현 금지 항목)
6. `spec/design-tokens.md` / `spec/component-catalog.md` (사용 가능 토큰·컴포넌트)
7. `spec/guards.md` (가드 활성/비활성 상태·영구 가드)

### 13-2. 세션 마무리 시 작업 세션 처리

1. SESSION_STATE.md 갱신 (현재 진행상황·다음 액션·본인 사전 준비 명시)
2. outline/_v*-build-guide.md 갱신 (다음 차수 빌드 가이드·있다면)
3. 본인 환경 파일 무결성 self-check 보고
4. 다음 세션 진입 시나리오 명시 (시나리오 A·B·C 등)

### 13-3. 세션 간 컨텍스트 복원 우선순위

- **필수**: SESSION_STATE.md (한 번에 컨텍스트 복원)
- **선택**: 직전 빌드 결과 스크린샷 (시각 평가 진행 중이면)
- **선택**: 외부 시점 또는 GPT 추가 회신 (있다면)

### 13-4. 작업 가드 유효성

가드 1~10은 본 파일 §3·§4·§5 절대/권장 규칙 영역에 직접 정합된 영구 가드.
가드 11 이후 누적 등록 + 활성/비활성 상태 + 영구 가드(11·45·49·53·60·65·66 영역)는 **`spec/guards.md`** 참조.

신규 세션 진입 시 영구 가드를 제외한 본 세션 한정 가드(11 이후)는 본인 디렉션에 따라 재선언 필요.

본 차수(v2.17) 이후 신규 가드는 본 §13-4 영역에 신규 라인으로 우선 등록하고, 분리 작업 차수에 `spec/guards.md`로 누적 이동한다.

### 13-5. 매트릭스 ↔ slides.yml 매핑 인지

매트릭스 step-page-matrix.md와 outline/slides.yml은 슬라이드 번호·슬러그 매핑이 정합하지 않을 수 있다. 작업 진입 전 충돌 발견 시 작업 중단 + 본인 보고.

매트릭스 §12-4 매핑 보정은 Step 1·2·3·4·5 영역 v10.14 정합 완료 (v2.15·v2.16 차수). slides.yml v1.0 → v2.0 골조 확장(42장·v2.15·v2.16 정합)은 발표 직전 일괄 진행 권장.

### 13-6. 매트릭스 v10 골조 확장 인지

매트릭스 v10은 slides.yml 28장 기준 통합 + 매트릭스 v9 1차 산출 15장 골조 본문 흡수 + §9-NEW 신규 페이지 §3 강제 명시 영역 추가. v10.14 시점 §9-NEW 작성 영역 = Step 1·2·3·4·5 본문 + 안내 페이지 2장 = 49장 정합 완료.

§9-NEW 차수별 작성 누적 영역(06·12·16·09·10·11·13·14·15·17·18·19·Step 3 14장·Step 4 9장·Step 5 9장)은 `docs/changelog.md` v2.0 ~ v2.16 라인 참조.

### 13-7. hp-num 일괄 보정 차수 보류 운영 메모

가드 21 정합 — hp-num(청중 노출 페이지 번호) 보정은 발표 직전 전체 빌드 완료 후 일괄 보정.

**일괄 보정 진입 시점**: 전체 49장 빌드 진입 차수 (Step 3·4·5 디자인 1차 재구성 + Step 2 잔여 보정 완료 후).
**일괄 보정 영역**: hp-num + (필요 시) deck data-slide·슬라이드 위치 재정렬.
**일괄 보정 가드**: 본문 영역(시각·텍스트·레이아웃) 변경 X — hp-num·data-slide만.

### 13-8. Step 2 잔여 영역 운영 메모

가드 40 정합 — Step 2 빌드 v3·v3.1 완료 후 잔여 영역 일괄 보정 보류.

**잔여 영역**:
- 2순위: 11 make-vs-buy·13 four-alternatives·16 lotte-vs-hyundai (v2 잔존·표 본문 60% 확대·아이콘 강화 필요)
- 3순위: 14·15·18·19 v3 텍스트 잔존 (분할 1에서 핵심만 재설계)

**일괄 보정 진입 시점 (권장)**: 발표 직전 일괄 보정 차수.

### 13-9 ~ 13-14. 차수별 진입 누적

Step 1 GPT 피드백·Step 3 빌드 v1 마감·Step 4·5 §9-NEW 작성·Step 1 v12·v13 진입 누적은 **`spec/session-log.md`** 참조.

신규 차수 진입 누적은 본 §13에 신규 §13-N 섹션으로 우선 기록하고, 분리 작업 차수에 `spec/session-log.md`로 누적 이동한다.

---

## §14. 신규 세션 진입 가이드

### 14-1. 신규 세션 시작 시 정독 영역 (우선순위 순)

1. **`CLAUDE.md`** (본 영역·§13·§14·운영 가이드)
2. **`SESSION_STATE.md`** (본 세션 종료 시점·미진입 영역·다음 차수)
3. **`outline/presentation-content-master.md`** (§1 발표 본문·§2 핵심 인용·§3 출처·§4 보조 논지·§5 표현 금지·§9 잔여 보정)
4. **`outline/step-page-matrix.md` §10 변경 이력 + §9-NEW** (본 세션 영역 인지·매트릭스 강제 명시 본문)
5. **`spec/guards.md`** (가드 활성/비활성 상태·영구 가드 명시)
6. **`dist/preview-step1.html` ~ `preview-step5.html`** (현 빌드 본문·필요 영역만)

### 14-2. 신규 세션 첫 차수 권장 진입 영역

**완료 영역 (v2.16 시점 누적)**:
- ✅ Step 1~5 §9-NEW 영역 모두 docx 본문 매핑 완료 (v2.16 정합 49장)
- ✅ Step 1~5 빌드 v1 전체 영역 완료 (총 36장 슬라이드 빌드)
- ✅ Step 4·5 안내 페이지 신규 추가 + 본론 헤더 시프트 (v2.16)
- ✅ CLAUDE.md 분리 작업 (v2.17)

**우선순위 1 — Step 3 디자인 1차 재구성**:
- dist/preview-step3.html v1 → v2 (14장 빌드·v10.13 확정 정합)
- 신규 7장 영역 빌드 + 기존 7장 페이지 번호 시프트 + 25↔26 위치 교환 + 출처 일괄 정정

**우선순위 2 — Step 4·5 dist 디자인 1차 재구성**:
- preview-step4.html v1 → v2 (9장) + preview-step5.html v1 → v2 (9장)
- 신규 안내 페이지 빌드 + 기존 8장씩 헤더 시프트 + 출처 일괄 정정 + 43 한계 보조 축소 + 52 출처 작은 source-line

**우선순위 3 — 발표 직전 잔여 영역** (가드 71 정합):
- 출처 §11 통합 (정합 ✗ 1건)
- hp-num 일괄 보정 (§13-7)
- content-thesis §2 보조 논지 4 본문 갱신
- presentation-content-master.md v1.4 → v1.5
- slides.yml v1.0 → v2.0 골조 확장 (49장 정합·§12-5 권장)
- 전체 통합 검토 + 발표 리허설
- 가드 66-3 발표 직전 통합 push (필수)

### 14-3. 신규 세션 첫 본인 메시지 영역 (예시)

```
신규 세션 진입. CLAUDE.md §14 신규 세션 진입 가이드 정합.

본 세션 시작 첫 차수 — 다음 영역 정독 부탁:
1. CLAUDE.md (현재 버전·§13·§14)
2. SESSION_STATE.md (본 세션 종료 시점·미진입 영역)
3. outline/presentation-content-master.md
4. outline/step-page-matrix.md §10 변경 이력 + §9-NEW
5. spec/guards.md (가드 활성/비활성 상태)
6. dist/preview-step*.html (필요 영역만)

정독 후 다음 영역 보고 부탁:
- 본 세션 누적 영역 인지 완료 여부
- 가드 11 이후 활성/비활성 영역 인지 완료 여부
- 다음 차수 진입 영역 권장
```

### 14-4. 가드 유효성 재선언 영역 (가드 62 정합)

신규 세션 진입 시 가드 11 이후는 본 세션 한정 영역 (영구 가드 제외). 신규 세션에서 다음 가드 영역 재선언 필요:

- **유지 권장 영역**: 가드 1~10 (영구 가드·CLAUDE.md §3·§4·§5 정합)·가드 11 (표현 금지)·가드 53·45·49 (디자인 보호)·가드 60 (3가지 문서 역할 분리)·가드 65·66 (Bash 배제·git push 운영)
- **재선언 영역**: 신규 세션 진입 차수의 본인 디렉션에 따라 가드 영역 재정의

영구 가드 상세는 `spec/guards.md` §0 참조.

### 14-5. 신규 세션 진입 안전 영역

본 세션 종료 시점·신규 세션 진입 영역 무결성 검증:

| 영역 | 본 세션 종료 시점 (v2.17) |
|---|---|
| 매트릭스 본문 | v10.14 §9-NEW 49장 (Step 1·2·3·4·5 본문 + 안내 2장) + §12-4 매핑 + §11 출처 |
| dist/preview-step1.html | v13 (10장·신규 slide 10 결론·hp-num 정합) |
| dist/preview-step2.html | v3.2 (11장·13 four-alternatives 보정) |
| dist/preview-step3.html | v1 (7장·다음 차수 v2 → 14장) |
| dist/preview-step4.html | v1 (8장·다음 차수 v2 → 9장·헤더 시프트) |
| dist/preview-step5.html | v1 (8장·다음 차수 v2 → 9장·헤더 시프트) |
| content-thesis | §1·§2 (보조 논지 4건)·§4 (표현 금지 5건)·§5 변경 이력 v10.14 |
| presentation-content-master | v1.4 |
| docs/md | Step1·2·3·4·5_v4.md 5개 (본인 pandoc 변환) |
| docs/changelog.md | v2.17 신설 (변경 이력 v1.0~v2.14 외부화) |
| spec/guards.md | v2.17 신설 (가드 11~89 누적 외부화) |
| spec/session-log.md | v2.17 신설 (§13-9~§13-14 진입 누적 외부화) |
| spec/git-push-flow.md | v2.17 신설 (§14-6 git push 흐름 외부화) |

### 14-6. git push 운영 흐름

상세는 `spec/git-push-flow.md` 참조 (가드 66·66-1·66-2·66-3 — push **시점** 규칙은 영구 유지·**실행 주체 제한은 2026.05.29 v2.18 해제**).

**요약**:
- 가드 66-1: Step 마감 시점 push (기본)
- 가드 66-2: 분할 마감 시점 push (선택)
- 가드 66-3: 발표 직전 통합 push (필수)
- **Claude Code가 git push 직접 수행 가능** (2026.05.29 v2.18·가드 65·66 해제). `§11` Bash 실행 규칙(foreground 기본·실행 전 선언·승인 원칙) 적용. 본인 직접 터미널 실행도 병행 가능.
- GitHub Pages: https://bapzzi.github.io/ERP_lottechemi/dist/preview-step*.html
